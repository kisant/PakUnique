<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_rules_menu">

    <TextView
        android:id="@+id/tv_rules"
        android:layout_width="147dp"
        android:layout_height="413dp"
        android:layout_marginTop="10dp"
        android:fontFamily="@font/rubik_one"
        android:text="There is matrix of where each cell cjnsists of a number. All the numbers looks same but there is a unique number which is different of all others. In the time span of 30 seconds, you have to select maximum unique numbers to maximise your score. For each correct selection. "
        android:textAlignment="center"
        android:textColor="#000000"
        android:textSize="@dimen/font_size_rules"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="@dimen/button_small_w"
        android:layout_height="@dimen/button_small_h"
        android:scaleType="fitCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/guideline5"
        app:srcCompat="@drawable/close" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline5"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.89" />
</androidx.constraintlayout.widget.ConstraintLayout>